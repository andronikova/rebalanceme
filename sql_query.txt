#from cs50:
CREATE TABLE IF NOT EXISTS 'users' ('id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, 'username' TEXT NOT NULL, 'hash' TEXT NOT NULL);
CREATE TABLE sqlite_sequence(name,seq);
CREATE UNIQUE INDEX 'username' ON "users" ("username");


# my
CREATE TABLE 'users' ('id' INTEGER PRIMARY KEY NOT NULL, 'username' TEXT NOT NULL, 'hash' TEXT NOT NULL);
INSERT INTO users (username,hash) VALUES ('test','abc');
SELECT * FROM users;

CREATE TABLE 'portfolio' ('userid' INTEGER NOT NULL, 'ticker' VARCHAR(255) NOT NULL, 'number' INTEGER NOT NULL, 'fraction' INTEGER NOT NULL, FOREIGN KEY(userid) REFERENCES users(id))
INSERT INTO portfolio (userid,ticker, number,fraction) VALUES (1,'AAPL',2, 50);
INSERT INTO portfolio (userid,ticker, number,fraction) VALUES (1,'NFLX', 3, 50);

SELECT * FROM portfolio;